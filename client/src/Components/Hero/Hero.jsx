import ListItems from "./ListItems";

import "./Hero.scss";

const Hero = () => {
  return (
    <div className="hero">
      <ListItems />
      <div className="carousel">eef</div>
    </div>
  );
};

export default Hero;
